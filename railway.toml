[build]
builder = "nixpacks"

[deploy]
startCommand = "node src/migrations/run.js && node src/server.js"
healthcheckPath = "/health"
restartPolicyType = "on_failure"

[environments.production.variables]
NIXPACKS_NODE_VERSION = "22"
